{
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:functions": "npx wrangler pages functions build functions --outdir dist",
    "deploy:cloudflare": "npm run build && npm run build:functions && npx wrangler pages deploy dist --project-name craig-benson-growth-constraint-scan --branch main",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "framer-motion": "latest",
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.5.15",
    "tailwindcss": "^3.4.17"
  }
}
